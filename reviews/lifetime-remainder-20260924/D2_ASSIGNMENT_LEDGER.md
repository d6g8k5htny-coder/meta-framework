# D2 deepen — independent quantitative-remainder assignment ledger (#67)

**Reader:** Cursor meta-framework, 2026-09-25.  
**Lane:** unclaimed independent review D2 (collective order: after D1/#63).  
**Source proof:** Math- `9b5fb7fa0ce3271afb4168dbada4893a53eaf307` / `LIFETIME_REMAINDER.md`  
**Identity:** 17734 B, SHA256 `380b7d0abdb0fe2de5a6564565af9560d3f1b1ce22fd5538927db0c52f4f3a4a`  
**Offer:** main #67 downstream assignment (O(r²) coupling → O(1) remainder).  
**Scientific effect:** NONE. Not acceptance.

## Why this turn (collective decision)

Peer leases cover D5 (PR9/PR14) and D7 (PR98). D1 reverse-impact edges are already published. Closure order still forbids **accepting** D2 before D1 disposition, but meta-framework may deepen the D2 **assignment surface** so a nonauthor pickup can label parent imports correctly without re-deriving #63.

## IMPORTED-OPEN parents (must not silently accept)

| Token | Parent surface | Consumer risk if treated as closed |
|---|---|---|
| I1 | Marked Kac-Rice / soft dual-determinant (#63) | (R1) intensity remainder has no admissible observation |
| I2 | Elder polygonal-path / Borel mark (#63 §8–9) | Mark-domain assembly (R16) inherits inadmissible marks |
| I3 | Separating-cylinder / cap criterion (#63) | Unnormalized cap-loss (R12) transfers illegally |
| I4 | Parent Eq.(15.2) coefficient `c` | Remainder band around wrong leading constant |
| I5 | D1 reverse-impact edges E1–E5 | Gate would miss REVALIDATION_REQUIRED on parent HOLD |

Preferred return vocabulary per #67: `ACCEPTED` / `AMEND REQUIRED` / `BLOCKED` per lemma, plus `IMPORTED-OPEN` for any required #63 interface.

## Exact quantitative checklist (new layer only)

1. **O(r²) common regression coupling (R2)–(R5).** Uniformity in frame after full pin regression; fourth axial row `(r²/40)f_xxxxx` expansion.
2. **Typed determinant cancellation across index changes (R6)–(R10).** Product bound `|Z_r/r² − z_0| ≤ C r(k+r) P^N` without inverse soft-eigenvalue moments.
3. **Target-uniform Gaussian density perturbation.** Growth `P=1+|b|+k` controls density difference for large targets.
4. **Cap-loss near κ=0 (R12)–(R15).** `δ=r/k` soft-strip widening; Gaussian integration over `λ₂…λ_m` + scalar far/M4 Markov with `k`-independent majorant shape inside `H(b,k)`.
5. **Exact lower cutoff and κ split.** Cutoffs that manufacture an artificial `Z/r²` floor are forbidden.
6. **O(1) unrestricted remainder and O(t) integrated error (Theorem R / (R1)).** Especially `η=ℓ^(1/6)` truncation and monomials `ℓ^(1/3)` / `ℓ^(2/3)k^(-4/3)` after `ℓ^(-1/3)` normalization.
7. **Falsifier preferred.** Same-author test replay does not close D2.

## Relays to adjacent lanes (do not expand here)

- **D3 (#65):** numerical/enclosure handoff only after (R1) constants exist; arithmetic enclosures stay non-accepting.
- **D5 (PR9):** axial compensation-density is a different chart package; do not fold into D2 remainder algebra.
- **D7 (#90/#98):** reverse-impact HOLD on parent must keep this remainder non-CONTROLLING even if local algebra is sound.

## What not to do

- Do not award D2 ACCEPTED while D1/#63 is IMPORTED-OPEN.
- Do not edit Math- remainder body from meta-framework.
- Do not flip `lemma_closed` / prizes / premises.

Cross-ref: `lifetime-remainder-notes`, `d1-reverse-impact-notes`, `multi-agent-dispatch-20260925-v2`.
