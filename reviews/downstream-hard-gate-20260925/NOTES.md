# Notes on DOWNSTREAM-HARD-GATE-20260925-v1 (Math- merged)

**Reader:** Cursor cloud agent on `meta-framework`, 2026-09-25.  
**Source tip:** Math- `baca69c394ab42130c61771bee74e808703f1ce7`  
**Object:** machine fail-closed promotion control for main #90/#86. Scientific effect: NONE.

## Accepted as engineering integrity

1. Non-discharge tokens (green CI, hashes, same-author review, navigation) cannot alone authorize CONTROLLING promotion.
2. Required `BLOCKED_ABSENT` historical carriers (`rnu_env.py`, Anthropic bundle zip, `allcell_fdz_enclosures.json`) force HOLD on dependents.
3. Reverse-impact uses union of old/new edges so deleting an edge cannot erase revalidation (hotfix `baca69c`).
4. D4 region complement correctly keeps mesoscopic / pin-collision / intermediate / witness-collision OPEN relative to fixed-remote.
5. Live graph reports `lemma_closed: false` and refuses promoting author-side candidates (#63 children, fixed-remote, PR87 before allowlist).

## Independent replay (this agent)

`python3 -B -S -m unittest -v test_hard_gate` → **32 OK** on the published tip (Python 3.12.3). Not analytic acceptance of any Math- candidate.

## Coordination holds

- Does not apply the D0 CI allowlist on `main` (Cursor main push still DENIED_403); patch remains in Math- `D0_CI_UNBLOCK.md`.
- Does not expand Math- PR7 / PR9 mesoscopic proof bodies from this repository.

Scientific status unchanged.
