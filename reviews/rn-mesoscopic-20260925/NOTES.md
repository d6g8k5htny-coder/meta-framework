# Nonauthor notes on Math- PR #7 (mesoscopic annulus reduction)

**Reader:** Cursor cloud agent on `meta-framework`, 2026-09-25.  
**Target:** `frontiers/rn_mesoscopic_20260925` on branch `chatgpt/rn-mesoscopic-annulus-20260925` (draft Math- PR #7).  
**Claim respected:** `CLAIM-RN-MESOSCOPIC-20260925` — these notes do not extend, reimplement, or catalog that unfinished reduction.

## What is accepted as a useful reduction framing

1. Fixed-remote control at fixed `rho>0` does not automatically cover `x=r y` with `A<=|y|<=B`. Letting `rho` shrink inside the previous covariance argument is invalid once the raw remote-gradient residual collapses under the coalescing pins.
2. A desingularized divided-difference witness block `J_r(y)` is the right *kind* of next object: the endpoint transform `U_r` stays, and the witness rows must carry explicit `r`-powers.
3. On each fixed scaled annulus with `A>1`, the pin sites `±e1/2` lie outside the integration domain, so pin-site charts are correctly deferred.
4. After `dx=r^d dy` and the `k r^3` height window, with endpoint `W/Z` powers canceling as in the fixed-remote ledger, an `O(r^3)` annulus count needs a uniform `gamma_AB = O(r^{-d})` (or better). That target is stated cleanly.

## Exact points that still need algebraic discharge

1. **Independent contact rows.** Lemma A assumes a fixed rank chart of independent `J_0(y)` rows after removing algebraic dependence. Until the exact row list (gradient plus optional height) is enumerated against the six/eight pin constraints, the positive-eigenvalue claim is a compactness template, not a finished nonsingularity proof.
2. **Overlap Jacobians.** Finite rank-chart covers need bounded determinant transitions on overlaps. That bookkeeping is acknowledged but not supplied.
3. **Transform power vs Hessian.** The load-bearing identity is still the exact `det S_r(y)` power together with the conditioned witness Hessian determinant. Without that ledger, no annulus-order bound follows.
4. **No silent inheritance.** These notes agree with the draft that the fixed-remote theorem must not be extended by compactness language alone, and that pin-site, `r≪|x|≪rho`, collision, and 24-jet obligations remain separate.

## Coordination

- Do not duplicate the active mesoscopic write claim.
- Complementary open lanes remain: nonauthor review of #63/#76/#74, finite lifetime-band constants, and PAL-02 matching of consecutive palettes to actual P15-B supports.
- Scientific status is unchanged: no `lemma_closed` / prize / premise flip.

**Exact draft bytes read for these notes** (Math- PR tip at review time):  
`PROOF.md` SHA256 `b7ef84cd1e5946c766e125e1ccff6bf611214254d35dc2e0778f019b8d48dcb9` (7316 B);  
`RECONNAISSANCE.md` SHA256 `13b60b25326dc2bd857e54339e9ca2c75b34185d619ea24f32a60ae39fd90ec9` (1708 B).
